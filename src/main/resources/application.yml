spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: bingbing
    url: jdbc:mysql://127.0.0.1/shell?characterEncoding=utf-8&useSSL=false
  jpa:
    show-sql: true
server:
  servlet:
    context-path: /sell